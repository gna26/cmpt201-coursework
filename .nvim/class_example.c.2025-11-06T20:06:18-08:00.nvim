#include <pthread.h>
#include <stdio.h>

void *func(void* string) {

  printf("hi");
  return NULL;
}

int main() {
  pthread_t thread1;

  pthread_create(&thread1, NULL, func, NULL);
  pthread_join(thread1, NULL);
}
